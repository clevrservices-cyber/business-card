# Local dev/deploy stack: postgres + backend API + frontend dev server.
#
# The frontend is deployed publicly through Lovable (its own dedicated URL) —
# cardreader-frontend here is local-only, for testing scan-api.ts against a
# real backend without touching the Lovable-hosted build. It intentionally
# carries no Traefik labels/network and is reached over its published port.
#
# The backend has no Lovable equivalent, so it still needs a real public
# route for the Lovable frontend to call. Its Traefik labels are included for
# consistency with this workspace's other apps but are NOT sufficient on
# their own — this box's Traefik runs file-provider only, so a dynamic route
# file is also needed. Until that's set up, reach it over the published port.
services:
  postgres:
    image: postgres:17-alpine
    container_name: cardreader-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-cardreader}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: ${POSTGRES_DB:-cardreader}
    ports:
      - "127.0.0.1:5439:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - cardreader-net

  cardreader-backend:
    build: ./backend
    container_name: cardreader-backend
    restart: unless-stopped
    working_dir: /app/backend
    volumes:
      - ./backend:/app/backend
      - backend-node-modules:/app/backend/node_modules
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      PORT: 8090
      DATABASE_URL: postgres://${POSTGRES_USER:-cardreader}:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}@postgres:5432/${POSTGRES_DB:-cardreader}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      ANTHROPIC_MODEL: ${ANTHROPIC_MODEL:-claude-sonnet-5}
      MAX_UPLOAD_MB: ${MAX_UPLOAD_MB:-15}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:3020}
    ports:
      - "8090:8090"
    depends_on:
      - postgres
    networks:
      - cardreader-net
      - traefik-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.cardreader-backend.rule=Host(`api-cardreader.arundopower.com`)"
      - "traefik.http.routers.cardreader-backend.entrypoints=websecure"
      - "traefik.http.services.cardreader-backend.loadbalancer.server.port=8090"

  cardreader-frontend:
    image: oven/bun:1-alpine
    container_name: cardreader-frontend
    restart: unless-stopped
    working_dir: /app
    volumes:
      - .:/app
      - frontend-node-modules:/app/node_modules
    command: sh -c "bun install && bun run dev -- --host 0.0.0.0 --port 3020"
    environment:
      VITE_API_URL: ${VITE_API_URL:-http://localhost:8090}
    ports:
      - "3020:3020"
    depends_on:
      - cardreader-backend
    networks:
      - cardreader-net

networks:
  cardreader-net:
  traefik-net:
    external: true

volumes:
  postgres-data:
  backend-node-modules:
  frontend-node-modules:
