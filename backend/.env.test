# Loaded automatically by `bun test` (NODE_ENV=test). Not a real credential —
# only DB-dependent tests (skipped when postgres isn't actually running) touch
# this connection string; the validation-only tests never open it.
DATABASE_URL=postgres://cardreader:cardreader_dev_pw@127.0.0.1:5439/cardreader
